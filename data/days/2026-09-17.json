{
  "schema_version": 2,
  "date": "2026-09-17",
  "id": "os-signals",
  "track": "OS",
  "track_name": "Operating Systems",
  "course": "CENG334",
  "title": "Signals",
  "sections": [
    {
      "heading": "The Signal Model",
      "body": "A **signal** is a one-word message delivered by the kernel to a process to notify it that some event has occurred; POSIX calls it a *software interrupt* by analogy with a hardware interrupt, except the direction is kernel to process rather than device to kernel. A signal carries exactly two pieces of information: its **number** (a small integer, 1-31 on Linux/POSIX, e.g. `SIGINT`=2, `SIGCHLD`=17 on Linux) and the fact that it arrived - there is no payload, no argument, and, critically, no count.\n\nSignals are generated from three sources: a hardware exception trapped by the kernel and translated into a signal (`SIGFPE` on divide-by-zero, `SIGSEGV` on an illegal memory reference, `SIGILL` on an illegal opcode); a kernel-detected software event (`SIGCHLD` when a child changes state, `SIGPIPE` on a write to a pipe with no reader, `SIGALRM` on timer expiry); or an explicit request from a process via `kill()`, `raise()`, or `pthread_kill()`. Every signal number has a **default action** - terminate, terminate with core dump, stop, continue, or ignore - that a process can override for every signal except `SIGKILL` and `SIGSTOP`, which the kernel enforces unconditionally so a process can always be stopped or killed from outside."
    },
    {
      "heading": "Kernel State: Pending and Blocked Bit Vectors, and Delivery on Return to User Mode",
      "body": "The kernel tracks each process's signal state with two fixed-width bit vectors kept in its process control block - in Linux's `task_struct` the fields are literally called `signal` and `blocked` - **pending**, one bit per signal type, set when that signal has been sent but not yet received, and **blocked**, the process's *signal mask*, set and cleared with `sigprocmask()` (or `pthread_sigmask()` per thread). Because each bit records only whether a signal of that type is outstanding, not how many, at most one instance of any signal number can be pending at a time: **signals are not queued**. If signal `k` is already pending and another signal `k` is sent, the kernel leaves the bit set - the second event leaves no additional trace anywhere in the process's state.\n\nThe kernel never delivers a signal in the middle of instruction execution. Delivery is checked only at transitions back to user mode: returning from a system call, an interrupt, or an exception. At that point the kernel computes `pnb = pending & ~blocked`, the set of pending, unblocked signals. If `pnb` is zero, control returns normally to the next instruction in the process's flow. Otherwise the kernel picks the least-numbered set bit `k`, clears bit `k` in `pending`, and rewrites the process's saved user-mode context - its `%rsp` and `%rip` on x86-64 - so that when control finally reaches user mode, execution begins at the handler for `k` instead of at the interrupted instruction; the interrupted `%rip` and a return trampoline are pushed onto the process's own stack so the handler can eventually resume it. The kernel repeats this check for every remaining nonzero bit in `pnb` before actually returning to user code, so several distinct pending signal types can each get one handler invocation back to back without any interrupted user instruction executing in between."
    },
    {
      "heading": "Installing Handlers: sigaction and Implicit Blocking",
      "body": "`signal()` is the historical, portably-inconsistent interface for installing a handler; modern code uses `sigaction(int signum, struct sigaction *act, struct sigaction *oldact)`. `struct sigaction` lets the caller specify, beyond the handler function pointer, an `sa_mask` - extra signals to block *while this handler runs*, on top of the implicit blocking described next - and `sa_flags`, most commonly `SA_RESTART`, which asks the kernel to transparently restart an interrupted blocking system call (`read`, `wait`, `accept`, ...) instead of failing it with `EINTR`.\n\nIndependent of `sa_mask`, the kernel always implicitly blocks a signal of the *same* number while its own handler is executing: a `SIGINT` handler cannot be reentered by a second `SIGINT` arriving mid-handler, because that second signal is left pending rather than delivered. This makes a handler non-reentrant with respect to its own signal by construction, but says nothing about *other* signal numbers, which can still interrupt a running handler - nested handlers - unless explicitly listed in `sa_mask`.\n\nWhen the handler executes its C `return`, control does not simply jump back to the interrupted instruction. The signal frame the kernel built contains a return address pointing at a small trampoline that makes the `rt_sigreturn` system call; that call restores the process's original register state and original blocked mask - undoing any implicit or `sa_mask` blocking - before finally resuming the instruction that was interrupted. This extra syscall is why signal delivery is measurably more expensive than a plain function call, and why code that leaves a handler by `longjmp` rather than `return` must restore the mask itself."
    },
    {
      "heading": "Async-Signal-Safety",
      "body": "A handler runs *concurrently* with whatever code it interrupted, sharing the same address space, the same global `errno`, and - in a single-threaded allocator - the same heap metadata. A function is **async-signal-safe** if it can be called from a handler no matter what the interrupted code was doing; POSIX publishes an explicit list that excludes almost all of stdio (`printf`), `malloc`/`free`, and most of libc, while including `write()`, `_exit()`, `kill()`, `signal()`, `sigaction()`, `sigprocmask()`, and, usefully, `waitpid()`.\n\nThe hazard is concrete. Suppose the main program calls `free(p)`, which is midway through unlinking `p` from the allocator's internal free list - it has updated one link pointer but not the other - when a signal arrives. The kernel checks `pnb`, delivers the signal, and control jumps to the handler with the free list in this half-updated state. If that handler itself calls `malloc()` for a scratch buffer, it walks the *same* free list, now inconsistent, and can return a chunk that overlaps live memory or crash while chasing a corrupted pointer. The handler finishes and returns via `rt_sigreturn`; `free()` resumes and completes its own update on top of damage the handler already did. The resulting corruption typically doesn't surface until a later, unrelated allocation, which is why the rule is absolute rather than situational: never call a non-async-signal-safe function from a handler.\n\nFor data shared between a handler and the main flow - a flag, a counter - the type must be `volatile sig_atomic_t`: `volatile` stops the compiler from caching the value in a register across a polling loop, and `sig_atomic_t` guarantees a single read or a single write is not itself interruptible mid-update. It does **not** make `flag++` atomic, since that is a read, an add and a write, three separate steps a handler can still land in the middle of."
    },
    {
      "heading": "The SIGCHLD Race and Why waitpid Must Loop",
      "body": "`SIGCHLD` is delivered whenever a child terminates or stops, and it inherits the same one-bit-per-type limitation as every other signal: it tells the parent that *at least one* child changed state, never *how many*. A handler that calls `waitpid()` exactly once - the natural first attempt - therefore reaps at most one zombie per *delivery*, not per *termination*. If several children die in a burst small enough that their termination events all find the pending bit already set before the parent's kernel-mode check has run even once, only one delivery occurs, one handler invocation happens, and one child is reaped; the rest sit as zombies, each holding a process-table slot, with no further `SIGCHLD` ever arriving to reap them. The worked example below traces this exactly.\n\nThe fix, standard since K&R-era UNIX and shown in the listing, is to drain every currently-reapable child inside the handler with a non-blocking loop - `while (waitpid(-1, &status, WNOHANG) > 0) { ... }` - so a single delivery, however many terminations coalesced into it, reaps everything actually waiting, and the loop exits cleanly once `waitpid()` returns `-1`/`ECHILD` or `0`."
    },
    {
      "heading": "Signals and Threads",
      "body": "POSIX signals were designed for single-threaded processes, so extending them to threads required choosing, per signal, whether it behaves as process-directed or thread-directed. **Disposition** - the handler function, or `SIG_IGN`/`SIG_DFL` - is process-wide: every thread shares one table, and installing a handler in one thread changes it for all of them. The **signal mask** (blocked set), by contrast, is per-thread, set with `pthread_sigmask()` rather than `sigprocmask()`, whose behaviour is undefined in a multithreaded process.\n\nSynchronous signals caused by a thread's own execution - `SIGSEGV` from that thread's bad dereference, `SIGFPE` from its own division - are always delivered to the thread that caused them. Asynchronous, process-directed signals (`SIGTERM`, `SIGCHLD`, a `kill()` from outside) are delivered to exactly one thread, chosen arbitrarily by the kernel from among those that do not currently have the signal blocked; if every thread blocks it, the signal simply stays pending on the process until some thread unblocks it.\n\nBecause an arbitrary thread taking an async-signal-unsafe action inside a handler is hard to reason about in a multithreaded program, the standard pattern avoids asynchronous handlers altogether: every thread blocks the signal via `pthread_sigmask(SIG_BLOCK, ...)`, and one dedicated thread calls `sigwait()` in a loop, receiving the signal synchronously as a return value rather than as an asynchronous control-flow diversion - sidestepping reentrancy concerns entirely."
    }
  ],
  "complexity": [
    {
      "operation": "sigaction() install/query",
      "time": "$O(1)$ syscall",
      "space": "one struct sigaction copied into the kernel's per-process disposition table",
      "notes": "replaces the table entry for that signal number; no allocation"
    },
    {
      "operation": "pnb = pending & ~blocked test on kernel to user-mode return",
      "time": "$O(1)$, one word compare over the signal-number range",
      "space": "two bit vectors per process (pending, blocked)",
      "notes": "performed on every return to user mode, not only right after a signal arrives"
    },
    {
      "operation": "handler dispatch (context rewrite)",
      "time": "one stack-frame push and register rewrite; no syscall needed to enter",
      "space": "one signal frame on the process's own user stack",
      "notes": "returning from the handler costs one extra syscall, rt_sigreturn, to restore mask and registers"
    },
    {
      "operation": "pthread_sigmask() / sigprocmask()",
      "time": "$O(1)$ syscall",
      "space": "one bitmask written, per-thread or per-process",
      "notes": "sigprocmask() is undefined once the process has more than one thread"
    },
    {
      "operation": "waitpid(-1, &status, WNOHANG) sweep inside a SIGCHLD handler",
      "time": "$O(1)$ amortized per zombie reaped",
      "space": "each un-reaped zombie holds one process-table slot",
      "notes": "must loop: a single delivery can represent many coalesced terminations"
    }
  ],
  "code": {
    "language": "c",
    "caption": "A correct SIGCHLD reaper: drain every zombie this delivery covers with an async-signal-safe waitpid(WNOHANG) loop",
    "source": "#include <stdio.h>\n#include <stdlib.h>\n#include <unistd.h>\n#include <signal.h>\n#include <sys/wait.h>\n#include <errno.h>\n\nvolatile sig_atomic_t reaped = 0;\n\nvoid sigchld_handler(int sig) {\n    (void)sig;\n    int olderrno = errno;\n    pid_t pid;\n    int status;\n    while ((pid = waitpid(-1, &status, WNOHANG)) > 0) {\n        reaped++;\n    }\n    errno = olderrno;\n}\n\nint main(void) {\n    struct sigaction sa;\n    sa.sa_handler = sigchld_handler;\n    sigemptyset(&sa.sa_mask);\n    sa.sa_flags = SA_RESTART;\n    if (sigaction(SIGCHLD, &sa, NULL) == -1) {\n        perror(\"sigaction\");\n        exit(1);\n    }\n\n    for (int i = 0; i < 3; i++) {\n        pid_t pid = fork();\n        if (pid == 0) {\n            _exit(i);\n        }\n    }\n\n    while (reaped < 3) {\n        pause();\n    }\n    printf(\"reaped %d children\\n\", reaped);\n    return 0;\n}"
  },
  "worked_example": {
    "problem": "A parent installs a `SIGCHLD` handler that calls `waitpid(-1, &status, 0)` **exactly once per invocation** (no `WNOHANG` loop). It then forks three children, C1, C2, C3, which all call `_exit()` in immediate succession, before the parent's kernel-mode pending/blocked check has run even once. Using the delivery algorithm from the mechanism sections above, trace what happens and determine how many children the handler reaps.",
    "solution": "Track the two bits that matter: `pending[SIGCHLD]` and whether `SIGCHLD` is currently blocked (it is, automatically, only while its own handler runs).\n\n1. **t0** - parent forks C1, C2, C3. `pending[SIGCHLD] = 0`, `SIGCHLD` unblocked.\n2. **t1** - C1 calls `_exit()`. The kernel sets `pending[SIGCHLD]: 0 -> 1`.\n3. **t2** - C2 calls `_exit()`. The kernel would set `pending[SIGCHLD]`, but it is already `1`; the bit has no room to record a second outstanding event, so C2's termination leaves no additional trace in the process's state.\n4. **t3** - C3 calls `_exit()`. Same as C2: `pending[SIGCHLD]` stays `1`.\n5. **t4** - the parent's next kernel-to-user-mode return: `pnb = pending & ~blocked = 1`. The kernel clears `pending[SIGCHLD]` (`1 -> 0`), rewrites the parent's context, and forces the handler to run. `SIGCHLD` is now implicitly blocked for the duration of this handler, so no second delivery can interrupt it.\n6. **t5** - the handler executes `waitpid(-1, &status, 0)`. Passing `-1` means \"any child\", so the kernel hands back whichever of {C1, C2, C3} it picks - exactly **one** zombie is reaped and removed from the process table. The handler returns.\n7. **t6** - before finally resuming user code, the kernel checks `pnb` once more: `pending[SIGCHLD]` is `0`, because no *new* termination has occurred since it was cleared at t4. No further delivery happens.\n\n**Result:** exactly **1 of the 3 children is reaped**; the other **2 remain zombies**, each still holding a process-table entry (pid and exit status), with no future `SIGCHLD` ever arriving to trigger their reaping. Replacing the single `waitpid()` call with the `WNOHANG` loop from the code listing fixes this: one delivery would then drain all three, regardless of how many terminations coalesced onto the single pending bit."
  },
  "pitfalls": [
    "Treating a signal as an event counter: because pending bits are not queued, several rapid occurrences of the same signal can collapse into one delivery, and a handler that reacts only once per invocation silently drops the rest (see the SIGCHLD worked example).",
    "Calling non-async-signal-safe functions - malloc, free, printf, most of libc - from a handler; if the interrupted code was mid-update of shared state (a heap free list, stdio's internal buffer) the handler's call can corrupt it, surfacing as a crash far away in unrelated code.",
    "Installing a handler without SA_RESTART and not checking for EINTR: a blocking system call (read, wait) that gets interrupted by any signal returns -1 with errno set to EINTR, and code that doesn't retry treats a spurious interruption as a real error.",
    "Assuming sigprocmask() in one thread blocks a signal process-wide: the signal mask is per-thread (pthread_sigmask()); disposition is process-wide, but blocking in one thread has no effect on delivery to a sibling thread with the signal unblocked.",
    "Sharing a plain int (not volatile sig_atomic_t) between a handler and the code it interrupts: the compiler is free to cache the value in a register across a polling loop, so the handler's write is never observed.",
    "Forgetting to save and restore errno inside a handler: any system call the handler makes can overwrite errno that the interrupted code had not yet read, corrupting its error handling after the handler returns."
  ],
  "applications": [
    "Shells (bash, zsh) install SIGCHLD handlers to reap background jobs asynchronously, without blocking the interactive prompt on a synchronous wait().",
    "Daemons and long-running servers catch SIGHUP to trigger a configuration reload in place, following the classic UNIX convention of not requiring a restart.",
    "Debuggers use SIGTRAP together with ptrace() to stop a target process at a breakpoint and hand control back to the debugger.",
    "Process supervisors (systemd, init systems, container runtimes) send SIGTERM and, after a grace period, SIGKILL, relying on SIGKILL's un-overridable default action as the last resort.",
    "Language runtimes install SIGSEGV handlers to distinguish an intentional guard-page trap - used for stack-growth checks or null-check elision - from a genuine illegal access."
  ],
  "exam_focus": [],
  "self_check": [
    {
      "question": "Why can a process not use a signal to reliably count how many times an event, such as a child terminating, has occurred?",
      "answer": "The kernel records a signal's occurrence with a single pending bit per signal number, not a counter: `pending[k]` can only be 0 or 1. If a signal of type k is already pending and another instance of type k is sent, the kernel leaves the bit set - the second (and any further) occurrence is simply discarded before the process ever sees it. So the most a handler for signal k can conclude from being invoked is that at least one instance of k arrived since the bit was last cleared, never exactly how many. Reliable counting requires the handler to actively query the real state each time it runs - for SIGCHLD this means draining `waitpid(WNOHANG)` in a loop rather than trusting the number of handler invocations."
    },
    {
      "question": "A parent installs a SIGCHLD handler that calls waitpid() exactly once (no WNOHANG loop) per invocation. It forks 5 children, all of which call _exit() before the parent's first kernel-mode pending/blocked check runs. How many of the 5 children does the handler reap, and what happens to the rest?",
      "answer": "Exactly 1 of the 5 is reaped. All five terminations occur before any delivery: the first _exit() sets pending[SIGCHLD] from 0 to 1, and each of the remaining four finds the bit already set and has no further effect on it - the pending bit cannot distinguish 1 outstanding termination from 5. When the parent next returns to user mode, pnb is 1, the kernel clears the bit and delivers exactly one invocation of the handler; that invocation calls waitpid(-1, &status, 0) once, which reaps one arbitrary child among the five zombies and returns. Because no new termination happens after the bit is cleared, no further SIGCHLD is ever delivered, so the remaining 4 children stay as zombies (dead but unreaped, each holding a process-table slot) indefinitely, unless something else explicitly calls waitpid() for them."
    },
    {
      "question": "Design a signal-safe way for a multithreaded server to handle SIGTERM (initiate a clean shutdown) without installing an asynchronous handler in every worker thread. What do you set up, and why does it avoid the async-signal-safety problems described above?",
      "answer": "At process startup, before spawning any worker threads, block SIGTERM in the initial thread with pthread_sigmask(SIG_BLOCK, &set, NULL); every thread created afterward inherits this mask, so SIGTERM stays blocked (never asynchronously delivered) everywhere. Spawn one dedicated thread whose only job is to call sigwait(&set, &sig) in a loop: sigwait() blocks synchronously until SIGTERM is pending, then atomically clears the pending bit and returns the signal number as an ordinary function return value - no handler function is invoked, and no user code is ever asynchronously interrupted. On return, that thread can safely call any function at all, including malloc, printf, or condition-variable signaling to tell the worker threads to wind down, because it is running as regular sequential code, not as a handler racing against the rest of the program. This sidesteps every hazard tied to async-signal-safety (reentrant allocator calls, non-atomic shared globals, errno clobbering) simply by never entering asynchronous handler context in the first place."
    }
  ],
  "key_points": [
    "Pending and blocked are per-process bit vectors, one bit per signal type; signals are never queued.",
    "The kernel checks pnb = pending & ~blocked only when returning to user mode, never mid-instruction.",
    "A handler's own return goes through the rt_sigreturn syscall to restore the original mask and registers.",
    "A signal is implicitly blocked while its own handler runs, so a handler can't be reentered by its own type.",
    "Only async-signal-safe functions (write, waitpid, _exit, kill, sigaction, ...) may be called inside a handler.",
    "Disposition is shared process-wide; the blocked mask is per-thread, set with pthread_sigmask()."
  ],
  "tagline": "Signals are one-bit-per-type notices delivered on return to user mode; never queued, and only async-signal-safe calls belong in a handler.",
  "tldr": "A signal is a one-word kernel-to-process notification tracked with per-process **pending** and **blocked** bit vectors; because each bit records only *whether*, never *how many*, signals are never queued. Delivery happens only when the kernel returns to user mode, where it computes `pending & ~blocked` and rewrites the process's context to run a handler instead of resuming normally. Handlers must stick to a short list of *async-signal-safe* functions, since they interrupt arbitrary code mid-update; `SIGCHLD` is the canonical case where forgetting this - and forgetting that one delivery may represent several coalesced terminations - leaks zombies.",
  "references": [
    "Bryant & O'Hallaron, Computer Systems: A Programmer's Perspective (3rd ed.), Exceptional Control Flow chapter",
    "Silberschatz, Galvin & Gagne, Operating System Concepts (10th ed.), Processes chapter (interprocess communication)",
    "Tanenbaum & Bos, Modern Operating Systems, Processes and Threads chapter",
    "POSIX.1-2001 (IEEE Std 1003.1), signal.h and the async-signal-safe functions list"
  ],
  "related_topics": [
    {
      "id": "os-process-api",
      "title": "Process API: fork, exec and wait",
      "track": "OS"
    },
    {
      "id": "os-threads",
      "title": "Threads and Threading Models",
      "track": "OS"
    },
    {
      "id": "os-ipc",
      "title": "Inter-Process Communication",
      "track": "OS"
    },
    {
      "id": "os-concurrency-bugs",
      "title": "Common Concurrency Bugs",
      "track": "OS"
    }
  ],
  "sources": [
    {
      "citation": "CENG334 lecture slides (2026) — CENG334-2026-W03aIPC, slides 49-51",
      "course": "CENG334",
      "kind": "course_slides"
    },
    {
      "citation": "CENG334 lecture slides (2026) — CENG334-2026-W03aIPC, slides 52-54",
      "course": "CENG334",
      "kind": "course_slides"
    },
    {
      "citation": "CENG334 lecture slides (2026) — CENG334-2026-W03aIPC, slides 34-37",
      "course": "CENG334",
      "kind": "course_slides"
    },
    {
      "citation": "CENG334 lecture slides (earlier years) — CENG334-2024-W03aIPC, slides 62-64",
      "course": "CENG334",
      "kind": "course_slides"
    },
    {
      "citation": "CENG334 lecture slides (earlier years) — CENG334-2024-W03aIPC, slides 68-71",
      "course": "CENG334",
      "kind": "course_slides"
    },
    {
      "citation": "CENG334 lecture slides (earlier years) — CENG334-2024-W03aIPC, slides 65-67",
      "course": "CENG334",
      "kind": "course_slides"
    },
    {
      "citation": "CENG334 lecture slides (2026) — CENG334-2026-W04threads, slides 52-54",
      "course": "CENG334",
      "kind": "course_slides"
    },
    {
      "citation": "CENG334 lecture slides (2026) — CENG334-2026-W02a-Processes, slides 9-10",
      "course": "CENG334",
      "kind": "course_slides"
    },
    {
      "citation": "Bryant & O'Hallaron, Computer Systems: A Programmer's Perspective (3rd ed.) — Randal E. Bryant_ David R. O’Hallaron-Computer Systems_ A Programmer’s Perspective-Pearson (2015), page 812",
      "course": "CENG331",
      "kind": "book"
    },
    {
      "citation": "Bryant & O'Hallaron, Computer Systems: A Programmer's Perspective (3rd ed.) — Randal E. Bryant_ David R. O’Hallaron-Computer Systems_ A Programmer’s Perspective-Pearson (2015), page 807",
      "course": "CENG331",
      "kind": "book"
    },
    {
      "citation": "Bryant & O'Hallaron, Computer Systems: A Programmer's Perspective (3rd ed.) — Randal E. Bryant_ David R. O’Hallaron-Computer Systems_ A Programmer’s Perspective-Pearson (2015), page 795",
      "course": "CENG331",
      "kind": "book"
    }
  ],
  "generated": {
    "provider": "claude-code",
    "model": "Claude Code (subscription)",
    "at": "2026-09-16T13:52:56+00:00",
    "pass": 1,
    "passages_offered": 14
  }
}
